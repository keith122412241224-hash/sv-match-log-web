# 期間環境レポートの表示修正

本番基点: `f91db9a87540ee53d7e067f140aa7dfdb0db4c6b`

- 「前期間からの環境変化」にPNG保存を追加。画像に見出し・対象期間・前期間・タイムゾーン・登録試合数を含める。
- 各変化の既存差分を符号付きの `%` 表記に変更。相対増減率への換算は行わない。
- 遭遇デッキと環境勝率グラフから前期間差分のpt表示を削除。
- Tier候補のカードから「自動Tier候補」「最終Tier」を削除し、デッキ名「不明」を除外。所属Tierは列見出しで示し、手動調整による列移動を維持する。

本番には対戦相手反転込みの環境勝率が導入済み。その集計、直接・反転の内訳表示、Tier評価、AIデータを維持する。ローカルmasterのファイル全体はコピーしない。

ランク帯・レート帯・DB・認証・依存関係・環境変数の変更は含めない。

検証結果:

- 本番基点のユニットテスト49件成功。既存の不明デッキ表示の期待値を今回の非表示仕様に更新。
- 型チェック・Lint・本番ビルド成功（18ページ生成）。
- 架空データとローカルSupabaseスタブで、390px/1440pxの画面、環境変化・環境勝率・Tier候補の実PNG保存、手動Tier変更とAIデータへの反映を確認。JavaScriptエラー・DB書き込み0件。
- 環境変化のPNG画像を目視確認し、見出し・比較期間・`+50%` の表示を確認。

ローカルの検証用ビルドにはダミー設定を使用しており、本番には配布しない。本番の管理者認証後の操作はこのローカル検証と区別する。

既存のGitHub main→Vercel Production連携で反映する。
